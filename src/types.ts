export type Candle = { time: number; open: number; high: number; low: number; close: number; volume: number };
export type Trade = { id: string; side: 'LONG' | 'SHORT'; entryTime: number; createdTime?: number; entry: number; size: number; sl: number; tp: number; exitTime?: number; exit?: number; grossResult?: number; fees?: number; entryFee?: number; result?: number; status: 'PENDING' | 'OPEN' | 'CLOSED'; orderType?: 'MARKET' | 'LIMIT'; leverage?: number; inputUnit?: 'USDT' | 'COIN'; inputValue?: number; makerFeePct?: number; takerFeePct?: number; slippagePct?: number; stopSlippagePct?: number; comment: string; outcome?: 'TP' | 'SL' | 'TIMEOUT' | 'MANUAL' };
export type Barrier = { id: string; entryTime: number; upper: number; lower: number; timeLimit: number; outcome?: 'TP' | 'SL' | 'TIMEOUT' };
export type SimulationSettings = { makerFeePct: number; takerFeePct: number; slippagePct: number; stopSlippagePct: number; showClosedTradeOverlays: boolean };
export type TrendLine = { id: string; datasetId: string; point1: { time: number; price: number }; point2: { time: number; price: number }; color: string; width: number; lineStyle: 'solid' | 'dashed' | 'dotted'; extendLeft: boolean; extendRight: boolean; showLabel: boolean; label: string; locked?: boolean };
export type Rectangle = { id: string; datasetId: string; timeLeft: number; timeRight: number; priceTop: number; priceBottom: number; borderColor: string; fillColor: string; fillOpacity: number; textColor: string; text?: string; borderWidth: number; borderStyle: 'solid' | 'dashed' | 'dotted'; locked: boolean };
export type Persisted = { datasets: { id: string; name: string; candles: Candle[] }[]; trades: Trade[]; annotations: Barrier[]; settings?: SimulationSettings; trendLines?: TrendLine[]; rectangles?: Rectangle[] };
