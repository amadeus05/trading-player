export type Candle = { time: number; open: number; high: number; low: number; close: number; volume: number };
export type Dataset = { id: string; name: string; candles: Candle[] };
export type Trade = { id: string; datasetId?: string; timeframeMinutes?: number; side: 'LONG' | 'SHORT'; entryTime: number; createdTime?: number; entry: number; size: number; sl: number; tp: number; exitTime?: number; exit?: number; grossResult?: number; fees?: number; entryFee?: number; result?: number; status: 'PENDING' | 'OPEN' | 'CLOSED'; orderType?: 'MARKET' | 'LIMIT'; leverage?: number; inputUnit?: 'USDT' | 'COIN'; inputValue?: number; makerFeePct?: number; takerFeePct?: number; slippagePct?: number; stopSlippagePct?: number; comment: string; outcome?: 'TP' | 'SL' | 'TIMEOUT' | 'MANUAL' };
export type Barrier = { id: string; entryTime: number; upper: number; lower: number; timeLimit: number; outcome?: 'TP' | 'SL' | 'TIMEOUT' };
export type AmbiguousExitPolicy = 'conservative' | 'optimistic' | 'ignore';
export type SimulationSettings = { makerFeePct: number; takerFeePct: number; slippagePct: number; stopSlippagePct: number; showClosedTradeOverlays: boolean; followCandle: boolean; ambiguousExitPolicy: AmbiguousExitPolicy };
export type AccountSettings = { initialBalance: number; quoteAsset: string };
export type TrendLine = { id: string; datasetId: string; point1: { time: number; price: number }; point2: { time: number; price: number }; color: string; textColor?: string; width: number; lineStyle: 'solid' | 'dashed' | 'dotted'; extendLeft: boolean; extendRight: boolean; showLabel: boolean; label: string; locked?: boolean };
export type Rectangle = { id: string; datasetId: string; timeLeft: number; timeRight: number; priceTop: number; priceBottom: number; borderColor: string; fillColor: string; fillOpacity: number; textColor: string; text?: string; borderWidth: number; borderStyle: 'solid' | 'dashed' | 'dotted'; locked: boolean };
export type FibonacciRetracement = { id: string; datasetId: string; point1: { time: number; price: number }; point2: { time: number; price: number }; showLabels?: boolean; locked?: boolean; lineWidth?: number; lineStyle?: 'solid' | 'dashed' | 'dotted'; trendColor?: string; trendWidth?: number; trendStyle?: 'solid' | 'dashed' | 'dotted' };
export type FibonacciTrendExtension = { id: string; datasetId: string; point1: { time: number; price: number }; point2: { time: number; price: number }; point3: { time: number; price: number }; showLabels?: boolean; locked?: boolean; lineWidth?: number; lineStyle?: 'solid' | 'dashed' | 'dotted' };
export type ParallelChannel = { id: string; datasetId: string; point1: { time: number; price: number }; point2: { time: number; price: number }; widthPoint: { time: number; price: number }; color: string; fillColor: string; fillOpacity: number; width: number; lineStyle: 'solid' | 'dashed' | 'dotted'; extendLeft: boolean; extendRight: boolean; locked?: boolean };
export type Persisted = { datasets: Dataset[]; trades: Trade[]; annotations: Barrier[]; lastDatasetId?: string; timeframeMinutes?: number; settings?: SimulationSettings; account?: AccountSettings; trendLines?: TrendLine[]; rectangles?: Rectangle[]; fibonacciRetracements?: FibonacciRetracement[]; fibonacciTrendExtensions?: FibonacciTrendExtension[]; parallelChannels?: ParallelChannel[] };
